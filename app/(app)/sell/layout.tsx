import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Sell for Naira - EngiPay",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
