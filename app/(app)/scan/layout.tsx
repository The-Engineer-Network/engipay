import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Scan to pay - EngiPay",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
